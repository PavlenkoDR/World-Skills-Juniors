<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyApp"
             x:Class="MyApp.MainPage"
                  Title="name">

    <MasterDetailPage.Detail>
        <NavigationPage>
            <x:Arguments>
                <local:View1/>
            </x:Arguments>
        </NavigationPage>
        
    </MasterDetailPage.Detail>
    
    <MasterDetailPage.Master>
        <ContentPage Title="name2">
            <ContentPage.Content>
                <StackLayout Padding="35, 15, 35, 15">
                    <ListView x:Name="listView1" ItemSelected="ListView1_ItemSelected" SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <StackLayout Padding="10">
                                        <Label Text="{Binding name}" FontSize="20" BackgroundColor="{Binding color}"/>
                                    </StackLayout>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>
            </ContentPage.Content>
        </ContentPage>
    </MasterDetailPage.Master>


</MasterDetailPage>
